---
"@xagentauth/server": minor
---

Add Rust server-side middleware with local HS256 JWT verification (jsonwebtoken crate), shared guard logic, Axum Tower layer + extractor, and Actix Transform middleware + extractor. Feature-gated behind "axum" and "actix" features.
